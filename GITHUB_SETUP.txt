# FiduciaMVP GitHub Setup Commands
# Run these commands AFTER creating the repository on GitHub

cd "C:\Users\curti\OneDrive\Desktop\WebDev\FiduciaMVP"

# Add the remote repository (update username if different)
git remote add origin https://github.com/curtiss18/FiduciaMVP.git

# Rename branch to main
git branch -M main

# Push to GitHub
git push -u origin main
